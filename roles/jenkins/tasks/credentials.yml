---

- name: Generate credential xml file
  template:
    src: "credentials.xml.j2"
    dest: "{{ jenkins_home }}/credentials.xml"
    owner: "{{ jenkins_process_user }}"
    group: "{{ jenkins_process_group }}"
    mode: 0640